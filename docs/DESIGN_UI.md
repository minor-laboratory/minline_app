# UI 설계

> 미니라인 앱의 화면 구조 및 상세 설계

**언제 읽어야 하는가:**
- 화면 구현 시작 전
- 위젯 구조 확인 시
- 레이아웃/인터랙션 확인 시

**참조:**
- [COMPONENT_SPECS.md](COMPONENT_SPECS.md) - **컴포넌트 구현 시 필수** (크기, 색상, 동작 상세)
- [../miniline/src/lib/components/](../miniline/src/lib/components/) - 웹 버전 컴포넌트 참조

---

## 화면 구조

### Timeline 중심 설계

**선택 이유:**
- 미니라인 핵심 가치: 짧은 생각을 빠르게 입력
- 입력에 집중하기 위한 단순한 구조
- Drafts/Posts는 보조 화면

**네비게이션:**
```
Timeline (메인)
├── AppBar
│   ├── [Drafts 뱃지]  → Drafts 화면
│   ├── [Posts]        → Posts 화면
│   └── [⋮] 메뉴       → Settings 등
└── Body
    ├── Fragment 리스트 (스크롤)
    └── 하단 고정 입력바
```

---

## Timeline 화면

### 레이아웃

```
┌─────────────────────────────────┐
│ MiniLine    [📋 3] [📄] [⋮]     │ ← AppBar
├─────────────────────────────────┤
│                                 │
│  ┌─────────────────────────┐   │
│  │ Fragment Card           │   │ ← Fragment 리스트
│  │ "오늘 아침에..."        │   │   (스크롤 가능)
│  │ 2024-11-12 09:30       │   │
│  └─────────────────────────┘   │
│                                 │
│  ┌─────────────────────────┐   │
│  │ Fragment Card           │   │
│  │ [image] "산책 중..."    │   │
│  │ 2024-11-12 10:15       │   │
│  └─────────────────────────┘   │
│                                 │
├─────────────────────────────────┤
│ ┌─┐ ┌─┐                        │
│ │ │ │ │  0/3   150/300   [저장]│ ← 하단 고정 입력바
│ └─┘ └─┘                        │   (이미지 프리뷰 있을 시)
└─────────────────────────────────┘
```

### AppBar

**구성:**
```dart
AppBar(
  title: Text('timeline.title'.tr()),
  actions: [
    // Drafts 뱃지
    Badge(
      label: Text('3'),  // Draft 개수
      child: IconButton(
        icon: Icon(AppIcons.drafts),
        onPressed: () => context.push('/drafts'),
      ),
    ),

    // Posts
    IconButton(
      icon: Icon(AppIcons.posts),
      onPressed: () => context.push('/posts'),
    ),

    // Settings 메뉴
    PopupMenuButton(...),
  ],
)
```

**주의:**
- Badge는 Draft 개수 > 0일 때만 표시
- 뱃지 색상: `colorScheme.primary`

### Fragment 리스트

**구성:**
```dart
ListView.builder(
  padding: EdgeInsets.only(
    bottom: FragmentInputBar.height + 16,  // 입력바 높이 + 여유
  ),
  itemBuilder: (context, index) {
    return FragmentCard(fragment: fragments[index]);
  },
)
```

**FragmentCard:**
- 최대 너비: 600px (태블릿 대응)
- 간격: 12px
- 패딩: 16px
- Border radius: 12px

### 하단 고정 입력바

**특징:**
- 채팅 앱 스타일 (하단 고정)
- `Column`의 마지막 자식으로 배치
- `SafeArea`로 시스템 바 영역 처리
- 키보드 올라올 때 자동 상승 (`resizeToAvoidBottomInset: true`)

**구조:**
```dart
Scaffold(
  resizeToAvoidBottomInset: true,
  body: Column(
    children: [
      FilterBar(),           // 검색/필터 (상단 고정)
      Expanded(child: FragmentList()),
      FragmentInputBar(),    // 입력바 (하단 고정)
    ],
  ),
)
```

**이유:** `bottomNavigationBar` 대신 Column 사용 (FilterBar 추가, 키보드 가림 방지)

---

## FragmentInputBar (입력 위젯)

### 기본 상태

```
┌─────────────────────────────────┐
│ Textarea (동적 높이)             │
│ "오늘의 생각을 입력하세요..."     │
├─────────────────────────────────┤
│ [📷] [0/300]          [저장]    │
└─────────────────────────────────┘
```

### 이미지 첨부 시

```
┌─────────────────────────────────┐
│ ┌──┐ ┌──┐                       │ ← 이미지 프리뷰 (80x80)
│ │  │ │  │                       │   우상단 X 버튼 (hover)
│ └──┘ └──┘                       │
│                                 │
│ Textarea                        │
│ "산책 중..."                    │
├─────────────────────────────────┤
│ [📷 2/3] [150/300]     [저장]  │
└─────────────────────────────────┘
```

### 제약사항

- **텍스트**: 최대 300자
- **이미지**: 최대 3개
- **텍스트 또는 이미지 하나는 필수**

**❌ 잘못된 검증**
```dart
if (content.isEmpty) {
  showError('내용을 입력하세요');
}
```

**✅ 올바른 검증**
```dart
if (content.trim().isEmpty && images.isEmpty) {
  showError('snap.content_or_media_required'.tr());
  return;
}
```

### 입력 방식

**텍스트:**
- `ShadInput` (multiline)
- `minLines: 1, maxLines: 3`
- 300자 제한 (`onChanged`에서 처리)
- Enter: 줄바꿈 (저장은 버튼만)

**이미지:**
1. 📷 버튼 탭 → ImagePicker
2. 갤러리에서 선택 (최대 3개)
3. 프리뷰 표시
4. X 버튼으로 삭제

**Drag & Drop:** 지원 안 함 (모바일)

**붙여넣기:**
```dart
onPaste: (event) {
  final items = event.clipboardData?.items;
  // 이미지 파일 감지 → 자동 첨부
}
```

### 상태 표시

**버튼:**
- 📷 버튼: 이미지 개수 표시 (`2/3`)
- 저장 버튼: 조건 충족 시 활성화

**로딩:**
- 저장 중: 버튼에 Loader 표시
- 이미지 업로드 중: "이미지 업로드 중..." 표시

---

## Drafts 화면

### 레이아웃

```
┌─────────────────────────────────┐
│ ← Drafts                        │ ← AppBar (뒤로가기)
├─────────────────────────────────┤
│  ┌─────────────────────────┐   │
│  │ Draft Card              │   │
│  │ "아침 루틴에 대한..."   │   │
│  │ 5 fragments • 2024-11-12│   │
│  └─────────────────────────┘   │
│                                 │
│  ┌─────────────────────────┐   │
│  │ Draft Card              │   │
│  │ "프로젝트 회고"         │   │
│  │ 3 fragments • 2024-11-11│   │
│  └─────────────────────────┘   │
└─────────────────────────────────┘
```

### DraftCard

**정보:**
- 제목 또는 첫 문장
- Fragment 개수
- 생성 날짜
- 상태 아이콘 (Draft/Editing)

**탭 시:**
- Draft 상세 화면으로 이동
- Fragment 목록 + AI 생성 초안 표시

---

## Posts 화면

### 레이아웃

```
┌─────────────────────────────────┐
│ ← Posts                         │
├─────────────────────────────────┤
│  ┌─────────────────────────┐   │
│  │ Post Card               │   │
│  │ "나의 아침 루틴"        │   │
│  │ 2024-11-12             │   │
│  └─────────────────────────┘   │
│                                 │
│  ┌─────────────────────────┐   │
│  │ Post Card               │   │
│  │ "프로젝트 회고록"       │   │
│  │ 2024-11-10             │   │
│  └─────────────────────────┘   │
└─────────────────────────────────┘
```

### PostCard

**정보:**
- 제목
- 발행 날짜
- 공개 상태 (Public/Private)

**탭 시:**
- Post 상세 화면 (읽기 모드)

---

## Settings 화면

### 레이아웃

```
┌─────────────────────────────────┐
│ ← Settings                      │
├─────────────────────────────────┤
│  ⚙️  일반                        │
│  ├ 언어                         │
│  ├ 테마 (라이트/다크/시스템)     │
│  └ 알림 설정                    │
│                                 │
│  🔔 알림                        │
│  ├ 입력 리마인더 (ON/OFF)       │
│  ├ 리마인더 시간 (20:00)        │
│  └ Draft 완성 알림 (ON/OFF)    │
│                                 │
│  👤 계정                        │
│  ├ 프로필                       │
│  ├ 구독 관리                    │
│  └ 로그아웃                     │
│                                 │
│  📱 앱 정보                      │
│  ├ 버전 (1.0.0)                │
│  ├ 이용약관                     │
│  └ 개인정보 처리방침            │
└─────────────────────────────────┘
```

### 알림 설정

**입력 리마인더:**
- 토글 (ON/OFF)
- 시간 선택 (TimePicker)
- 반복: 매일

**Draft 완성 알림:**
- 토글 (ON/OFF)
- 푸시 알림 권한 확인 필요

---

## 공통 위젯

### Loading

**전체 화면:**
```dart
Center(
  child: CircularProgressIndicator(),
)
```

**인라인:**
```dart
SizedBox(
  width: 16,
  height: 16,
  child: CircularProgressIndicator(strokeWidth: 2),
)
```

### Empty State

```dart
Center(
  child: Column(
    children: [
      Icon(AppIcons.info, size: 48),
      SizedBox(height: 16),
      Text('fragments.empty'.tr()),
    ],
  ),
)
```

### Error Toast

```dart
toastStore.error('sync.failed'.tr());
```

---

## 테마 및 스타일

### 색상

**minorlab_common 테마 사용:**
```dart
final colorScheme = Theme.of(context).colorScheme;

primary: colorScheme.primary
onPrimary: colorScheme.onPrimary
surface: colorScheme.surface
onSurface: colorScheme.onSurface
```

**❌ 하드코딩 금지**
```dart
Container(color: Colors.blue)
Text('Title', style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold))
```

**✅ 테마 사용**
```dart
Container(color: colorScheme.primary)
Text('Title', style: Theme.of(context).textTheme.titleLarge)
```

### 타이포그래피

```dart
titleLarge: 22px, w600  // 화면 제목
titleMedium: 16px, w600 // 카드 제목
bodyLarge: 16px, w400   // 본문
bodyMedium: 14px, w400  // 부가 정보
labelSmall: 12px, w500  // 라벨
```

### 간격

```dart
xs: 4px
sm: 8px
md: 12px
lg: 16px
xl: 24px
xxl: 32px
```

**minorlab_common 사용:**
```dart
padding: EdgeInsets.all(Spacing.lg)
gap: Spacing.md
```

### Border Radius

```dart
sm: 8px   // Chip
md: 12px  // Card
lg: 16px  // Button, Input
xl: 20px  // Dialog
```

---

## 인터랙션

### 탭 피드백

**카드:**
- 탭 시: Material ripple 효과
- 배경색 변화 없음 (리플만)

**버튼:**
- 탭 시: scale 0.95 → 1.0 (100ms)
- 또는 Material elevation 변화

### 스크롤

**Timeline:**
- Pull to refresh (선택사항)
- 무한 스크롤 (페이지네이션)

**리스트:**
- 스크롤바 자동 표시/숨김
- Over-scroll 효과 (iOS bounce, Android glow)

### 키보드

**입력 중:**
- 키보드 올라올 때 입력바 자동 상승
- 리스트 스크롤 유지

**완료 시:**
- 키보드 내림
- 입력창 초기화

---

## 반응형 (태블릿)

### 화면 크기별 레이아웃

**모바일 (<600px):**
- 전체 너비 사용
- 단일 컬럼

**태블릿 (≥600px):**
- 최대 너비: 600px
- 중앙 정렬
- 좌우 패딩: 24px

```dart
Container(
  constraints: BoxConstraints(maxWidth: 600),
  child: FragmentList(),
)
```

---

## 자주 하는 실수

1. **하드코딩된 색상/텍스트** → 테마 시스템, `.tr()` 사용
2. **고정 높이 입력창** → 동적 높이 조정 필요
3. **이미지 업로드 실패 시 저장 차단** → 로컬 저장 우선
4. **키보드 올라올 때 레이아웃 깨짐** → `resizeToAvoidBottomInset: true`
5. **Pull to refresh 없음** → 수동 새로고침 방법 제공 필요

---

**작성 후 확인:**
- [x] ❌/✅ 패턴 사용했는가?
- [x] "언제 읽어야 하는지" 명시했는가?
- [x] 전체 코드 복사하지 않았는가?
- [x] 주석 최소화했는가?
